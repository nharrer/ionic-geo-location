import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.netzgewitter.ionicgeolocation',
  appName: 'ionic-geo-location',
  webDir: 'www'
};

export default config;
